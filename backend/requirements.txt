# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pgvector==0.3.6

# Pydantic / settings
pydantic[email]==2.10.3
pydantic-settings==2.6.1

# Task queue
celery[redis]==5.4.0
redis==5.2.1

# HTTP client
httpx==0.28.1

# AI providers
anthropic==0.40.0
openai==1.57.4

# Security
cryptography==44.0.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.18

# Payments
stripe==11.4.1

# Email
sendgrid==6.11.0

# Resume parsing
pdfplumber==0.11.4
python-docx==1.1.2
docx2txt==0.9
numpy==2.4.4

# Web scraping / RAG
# crawl4ai is intentionally excluded from the production image — it installs
# Playwright browser binaries via a post-install hook which fails in Railway's
# build environment. The RAG pipeline falls back automatically to httpx +
# BeautifulSoup when crawl4ai is not importable (see rag_pipeline._crawl).

# Templating
jinja2==3.1.4
